# ================================
# APP CONFIG
# ================================

NEXT_PUBLIC_APP_URL=https://your-deployed-app-url.example
NEXTAUTH_URL=https://your-deployed-app-url.example

# Generate secure secrets with: `openssl rand -base64 32` or a secrets manager
# Primary JWT secret (preferred). If present, used for signing/verifying tokens.
JWT_SECRET=replace_with_long_random_jwt_secret

# Backward-compatible secrets (optional) — keep equal to `JWT_SECRET` if used
ADMIN_SECRET=replace_with_long_random_admin_secret
NEXTAUTH_SECRET=replace_with_long_random_nextauth_secret


# ================================
# DATABASE (MongoDB)
# ================================

MONGODB_URI=mongodb+srv://<db_user>:<db_password>@cluster0.mongodb.net/solange_bookings?retryWrites=true&w=majority

# ================================
# STRIPE PAYMENTS
# ================================

NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_your_key_here
STRIPE_SECRET_KEY=sk_test_your_key_here


# ================================
# PAYPAL PAYMENTS
# ================================

NEXT_PUBLIC_PAYPAL_CLIENT_ID=your_paypal_client_id


# ================================
# LIVE CHAT (optional)
# ================================

NEXT_PUBLIC_TAWK_PROPERTY_ID=your_tawk_property_id


# ================================
# EMAIL (SMTP - Titan Email)
# ================================

SMTP_HOST=smtp.titan.email
SMTP_PORT=465
SMTP_USER=info@audienceaura.org
SMTP_PASS=replace_with_smtp_password

# Admin seed credentials (used by `/api/auth/seed`).
# Call `/api/auth/seed` once after deploying, then rotate or remove these.
ADMIN_SEED_EMAIL=admin@solange.salon
ADMIN_SEED_PASSWORD=replace_with_strong_password